<h1>新規投稿</h1>

<%= form_with model: @post, url:posts_path, local:true do |f| %>
  <h4>名前</h4>
  <%= f.text_field :name %>
  <h4>場所</h4>
  <%= f.text_field :location %>
  <%= f.submit '投稿' %>
<% end %>